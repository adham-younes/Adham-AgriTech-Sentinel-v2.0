# متغيرات البيئة - Environment Variables

## 📋 نظرة عامة

هذا المستند يوثق جميع متغيرات البيئة المطلوبة لتشغيل منصة Adham AgriTech.

## 🔑 المتغيرات المُكونة حالياً

### 1. OpenWeather API
**الحالة:** ⚠️ يحتاج للتحقق

```env
OPENWEATHER_API_KEY=your_api_key_here
```

**الاستخدام:**
- جلب بيانات الطقس الحالية والتنبؤات
- مراقبة درجات الحرارة والرطوبة
- تنبيهات الطقس القاسي

**كيفية الحصول عليه:**
1. سجل في https://openweathermap.org/api
2. احصل على API key من لوحة التحكم
3. اختر الخطة المناسبة (Free tier: 60 calls/minute)

**اختبار الاتصال:**
```bash
curl "https://api.openweathermap.org/data/2.5/weather?q=Cairo&appid=YOUR_API_KEY"
```

---

### 2. OpenAI API
**الحالة:** ❌ لا يعمل - يحتاج تفعيل

```env
OPENAI_API_KEY=sk-...
```

**الاستخدام:**
- المساعد الذكي للمزارعين
- تحليل النصوص والتوصيات
- معالجة اللغة الطبيعية

**المشاكل المحتملة:**
- ❌ المفتاح غير صالح
- ❌ لا يوجد رصيد في الحساب
- ❌ تم تجاوز حد الاستخدام

**الحل:**
1. تحقق من صلاحية المفتاح في https://platform.openai.com/api-keys
2. تأكد من وجود رصيد في https://platform.openai.com/account/billing
3. راجع حدود الاستخدام في https://platform.openai.com/account/limits

**اختبار الاتصال:**
```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"
```

---

### 3. EOSDA Platform (Earth Observing System Data API)
**الحالة:** ✅ مُكوّن

```env
EOSDA_API_KEY=apk.cefa9921669b0857be282894813d1213ed88c5e8299e29a5e91db105464aa232
EOSDA_API_BASE_URL=https://api.eos.com/api/data/v1
```

**الاستخدام:**
- 🔎 البحث عن مشاهد الأقمار الصناعية لـ AOI
- 📊 الحصول على بيانات إحصائيات مؤشرات النبات
- 🥬 تصور مؤشرات النبات لـ AOI
- 🏞 عرض الصور النقطية مع قناع السحب
- 💦 الحصول على بيانات رطوبة التربة
- 🌦 الحصول على بيانات الطقس (التنبؤ، التاريخي، الحالي)
- 🗺 إنشاء خرائط الغطاء النباتي والإنتاجية
- ⛰ عرض خرائط DEM

**الميزات:**
- صور الأقمار الصناعية عالية الدقة
- حساب مؤشرات NDVI وبيانات الغطاء النباتي
- مراقبة صحة المحاصيل وتحليلات النمو
- بيانات طقس شاملة
- إحصائيات مفصلة للمؤشرات

**كيفية الحصول عليه:**
1. سجل في https://eos.com/api/
2. احصل على API Key من لوحة التحكم
3. راجع الوثائق في https://doc.eos.com/docs/

**الوثائق:**
- [EOSDA API Connect](https://doc.eos.com/docs/start-build-with-EOSDA-API-Connect/)
- [Search API](https://doc.eos.com/docs/search-api/)
- [Imagery API](https://doc.eos.com/docs/imagery-api/)
- [Statistics API](https://doc.eos.com/docs/statistics-api/)
- [Weather API](https://doc.eos.com/docs/weather-api/)

**اختبار الاتصال:**
```bash
curl -X GET "https://api.eos.com/api/data/v1/search" \
  -H "API-KEY: $EOSDA_API_KEY"
```

---

### 4. ESD Platform (Earth Satellite Data) - Fallback
**الحالة:** ⚠️ احتياطي

```env
ESD_CLIENT_ID=your_client_id
ESD_CLIENT_SECRET=your_client_secret
ESD_AUTH_URL=https://auth.esd.earth/oauth/token
ESD_API_BASE_URL=https://api.esd.earth/v1
```

**الاستخدام:**
- احتياطي إذا فشل EOSDA
- صور الأقمار الصناعية عالية الدقة
- حساب مؤشرات NDVI وبيانات الغطاء النباتي

**كيفية الحصول عليه:**
1. سجل في بوابة ESD على https://portal.esd.earth/
2. أنشئ تطبيق جديد واحصل على بيانات المصادقة

**اختبار الاتصال:**
```bash
curl -X POST "$ESD_AUTH_URL" \
  -d "client_id=$ESD_CLIENT_ID" \
  -d "client_secret=$ESD_CLIENT_SECRET" \
  -d "grant_type=client_credentials"
```

---

### 5. Mapbox
**الحالة:** ⚠️ يحتاج للتحقق

```env
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.eyJ1...
```

**الاستخدام:**
- عرض الخرائط التفاعلية
- تحديد مواقع الحقول
- Geocoding والبحث عن المواقع

**كيفية الحصول عليه:**
1. سجل في https://account.mapbox.com/
2. احصل على Access Token من https://account.mapbox.com/access-tokens/
3. استخدم Public token (يبدأ بـ pk.)

**اختبار الاتصال:**
```bash
curl "https://api.mapbox.com/geocoding/v5/mapbox.places/Cairo.json?access_token=$MAPBOX_TOKEN"
```

---

### 6. Supabase
**الحالة:** ⚠️ يحتاج للتحقق

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**الاستخدام:**
- قاعدة البيانات الرئيسية
- المصادقة والتفويض
- تخزين الملفات

**كيفية الحصول عليه:**
1. أنشئ مشروع في https://supabase.com/dashboard
2. احصل على URL و anon key من Project Settings → API

**اختبار الاتصال:**
```bash
curl "$SUPABASE_URL/rest/v1/" \
  -H "apikey: $SUPABASE_ANON_KEY" \
  -H "Authorization: Bearer $SUPABASE_ANON_KEY"
```

---

## 🔧 المتغيرات المفقودة

### Groq AI (اختياري)
```env
GROQ_API_KEY=gsk_...
```

**الاستخدام:**
- بديل أسرع وأرخص لـ OpenAI
- نماذج LLM مفتوحة المصدر

**كيفية الحصول عليه:**
https://console.groq.com/keys

---

### Infura (Blockchain)
```env
NEXT_PUBLIC_INFURA_API_KEY=your_infura_key
NEXT_PUBLIC_SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_KEY
```

**الاستخدام:**
- الاتصال بشبكة Ethereum
- معاملات البلوكتشين
- NFTs للأراضي الزراعية

---

## 🚀 تشغيل اختبار التكاملات

### عبر GitHub Actions:
```bash
gh workflow run verify-integrations.yml
```

### محلياً:
```bash
# اختبار OpenWeather
curl "https://api.openweathermap.org/data/2.5/weather?q=Cairo&appid=$OPENWEATHER_API_KEY"

# اختبار OpenAI
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# اختبار ESD
curl -X POST "$ESD_AUTH_URL" \
  -d "client_id=$ESD_CLIENT_ID" \
  -d "client_secret=$ESD_CLIENT_SECRET" \
  -d "grant_type=client_credentials"
```

---

## 📊 حالة التكاملات

| الخدمة | الحالة | الأولوية | الملاحظات |
|--------|--------|----------|-----------|
| OpenWeather | ⚠️ غير مختبر | عالية | مطلوب لبيانات الطقس |
| OpenAI | ❌ لا يعمل | حرجة | المساعد الذكي معطل |
| ESD Platform | ⚠️ غير مختبر | عالية | صور الأقمار الصناعية |
| Mapbox | ⚠️ غير مختبر | متوسطة | الخرائط التفاعلية |
| Supabase | ⚠️ غير مختبر | حرجة | قاعدة البيانات الرئيسية |
| Groq AI | ❌ غير مُكون | منخفضة | بديل اختياري |
| Infura | ❌ غير مُكون | منخفضة | للبلوكتشين |

---

## 🔒 أمان المتغيرات

### ✅ الممارسات الجيدة:
- ✅ استخدم Vercel Environment Variables للإنتاج
- ✅ استخدم `.env.local` للتطوير المحلي
- ✅ لا تضع المتغيرات في Git
- ✅ استخدم `NEXT_PUBLIC_` فقط للمتغيرات العامة

### ❌ تجنب:
- ❌ وضع API keys في الكود
- ❌ مشاركة `.env` files
- ❌ استخدام نفس المفاتيح للتطوير والإنتاج

---

## 📝 الخطوات التالية

1. **فوري:**
   - [ ] إصلاح OpenAI API (تفعيل الفوترة)
   - [ ] اختبار جميع المتغيرات الموجودة
   - [ ] توثيق أي أخطاء

2. **قريب:**
   - [ ] إضافة Groq AI كبديل
   - [ ] تفعيل Infura للبلوكتشين
   - [ ] إعداد monitoring للتكاملات

3. **مستقبلي:**
   - [ ] إضافة Google Earth Engine
   - [ ] تكامل مع خدمات IoT
   - [ ] إضافة خدمات SMS للتنبيهات

---

**آخر تحديث:** 2025-10-31  
**المسؤول:** CODEX Integration Verification System
